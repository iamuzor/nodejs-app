~~* Dockerize `./src/express-app` as `framble_express_app:v1`~~
~~* Dockerize `./apps/betting`, using `framble_express_app:v1` as base image~~

Decided to package as an NPM package instead of Dockerizing.